# yaml-language-server: $schema=https://json.schemastore.org/github-workflow.json

name: Publish

on:
  push:
    tags:
      - "v*.*.*"
      - "*/v*.*.*"

permissions:
  contents: write
  id-token: write
  pages: write

concurrency:
  group: publish-${{ github.ref }}
  cancel-in-progress: true

jobs:
  detect:
    name: Detect package to publish
    runs-on: ubuntu-latest
    outputs:
      package: ${{ steps.detect.outputs.package }}
      version: ${{ steps.detect.outputs.version }}
      filter: ${{ steps.detect.outputs.filter }}
    steps:
      - name: Parse tag
        id: detect
        run: |
          tag="${GITHUB_REF_NAME}"

          if [[ "$tag" =~ ^v([0-9]+\.[0-9]+\.[0-9]+.*)$ ]]; then
            echo "package=@smounters/imperium" >> "$GITHUB_OUTPUT"
            echo "version=${BASH_REMATCH[1]}" >> "$GITHUB_OUTPUT"
            echo "filter=@smounters/imperium" >> "$GITHUB_OUTPUT"
          elif [[ "$tag" =~ ^imperium-cron/v([0-9]+\.[0-9]+\.[0-9]+.*)$ ]]; then
            echo "package=@smounters/imperium-cron" >> "$GITHUB_OUTPUT"
            echo "version=${BASH_REMATCH[1]}" >> "$GITHUB_OUTPUT"
            echo "filter=@smounters/imperium-cron" >> "$GITHUB_OUTPUT"
          elif [[ "$tag" =~ ^imperium-events/v([0-9]+\.[0-9]+\.[0-9]+.*)$ ]]; then
            echo "package=@smounters/imperium-events" >> "$GITHUB_OUTPUT"
            echo "version=${BASH_REMATCH[1]}" >> "$GITHUB_OUTPUT"
            echo "filter=@smounters/imperium-events" >> "$GITHUB_OUTPUT"
          else
            echo "::error::Unknown tag format '${tag}'. Use vX.Y.Z, imperium-cron/vX.Y.Z, or imperium-events/vX.Y.Z"
            exit 1
          fi

  publish:
    name: Publish ${{ needs.detect.outputs.package }}
    needs: detect
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Setup pnpm
        uses: pnpm/action-setup@v4
        with:
          version: 10

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: pnpm
          registry-url: https://registry.npmjs.org

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Sync version
        run: pnpm --filter "${{ needs.detect.outputs.filter }}" exec pnpm version "${{ needs.detect.outputs.version }}" --no-git-tag-version --allow-same-version

      - name: Build core (required by all packages)
        run: pnpm --filter @smounters/imperium run build

      - name: Typecheck
        run: pnpm --filter "${{ needs.detect.outputs.filter }}" run typecheck

      - name: Test
        run: pnpm --filter "${{ needs.detect.outputs.filter }}" run test

      - name: Build
        run: pnpm --filter "${{ needs.detect.outputs.filter }}" run build

      - name: Publish to npm
        run: pnpm --filter "${{ needs.detect.outputs.filter }}" publish --access public --no-git-checks
        env:
          NODE_AUTH_TOKEN: ${{ secrets.NPM_TOKEN }}

  docs:
    name: Build & deploy docs
    needs: [detect, publish]
    if: needs.detect.outputs.package == '@smounters/imperium'
    runs-on: ubuntu-latest
    env:
      DOCS_BASE: /${{ github.event.repository.name }}/
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Setup pnpm
        uses: pnpm/action-setup@v4
        with:
          version: 10

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: pnpm

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Build docs
        run: pnpm run docs:build

      - name: Upload docs artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: packages/imperium/docs/.vitepress/dist

  deploy_docs:
    name: Deploy docs
    needs: docs
    runs-on: ubuntu-latest
    environment:
      name: github-pages
    steps:
      - name: Deploy to GitHub Pages
        uses: actions/deploy-pages@v4

  release:
    name: Create release
    needs: [detect, publish]
    runs-on: ubuntu-latest
    steps:
      - name: Create release
        uses: softprops/action-gh-release@v2
        with:
          tag_name: ${{ github.ref_name }}
          generate_release_notes: true
